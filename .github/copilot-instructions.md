# Copilot Instructions

## Project Guidelines
- User prefers minimal file creation and wants changes consolidated into existing files whenever possible, creating new files only when strictly necessary.

## Code Style
- Avoid lambdas everywhere; prefer named methods/functions instead.
- Use very descriptive variable names in viewmodels (e.g., avoid generic names like 'snapshot').